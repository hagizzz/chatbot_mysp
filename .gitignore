# ===== Bí mật =====
.env
.env.*
!.env.example
!.env.staging.example
google-service-account.json
fb_ads_tokens.json
pages.json

# ===== Phụ thuộc =====
node_modules/
__pycache__/
*.pyc
.venv/

# ===== Mô hình / chỉ mục lớn (tái tạo được bằng build_*.py) =====
*.npz
*.traineddata
hash_index.json
celeb_index.json

# ===== Dữ liệu chạy thật (theo máy, không đưa vào git) =====
conversation_memory.json
conversation_memory.db
conversation_memory.db-*
processed_messages.json
orders_state.json
pending_followups.json
bot_dup_sent.json
ad_learned_map.json
video_index.json
data/

# ===== Log =====
log*.txt
botlog/
*.log
soi_nhan_log.txt
lichsu_log.txt

# ===== Bản sao lưu / rác tạm =====
*.bak
*.bak-*
*.orig
*.tmp
dump.txt
fresh.txt
raw.txt
test/ca_vang/tra_cuu_conv.local.json
*.local.json
data/

# Mốc bỏ qua tin cũ theo từng hội thoại (reset_hoi_thoai.js ghi)
moc_bo_qua.json

# Khoá chống hai bot cùng chạy (PID của máy này)
bot.lock

# ===== Dọn 27/08/2026 =====
# Kho lưu trữ: bản sao lưu, log cũ, tệp rỗng do gõ nhầm. Chỉ DỜI vào đây chứ không
# xoá, nên nó chứa cả .env.staging.bak (có bí mật) và ~70 MB chỉ mục build lại được.
# Tuyệt đối không đưa lên git.
luu_tru/

# Log hội thoại THẬT — dữ liệu nguồn của bộ ca vàng (test/trich_ca_vang.js đọc).
# Có tên và tin nhắn của khách thật, không được đưa lên git.
test/log_that/
